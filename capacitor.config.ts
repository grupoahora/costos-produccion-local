import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tuempresa.costosproduccion',
  appName: 'Costos Producción',
  webDir: 'dist',
};

export default config;
